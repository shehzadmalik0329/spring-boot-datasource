package com.example.demo.player;

public record Player(Integer id,
                     String name,
                     Integer ranking,
                     String team,
                     double age) {
}
